<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- MAP関係必要なもの -->
    <uses-permission android:name="android.permission.INTERNET"/> <!--オンラインでの利用も可能に-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> <!--高精度な位置情報取得-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!--WIFIや携帯基地局から位置情報取得（精度低）-->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> <!--Android10以降バックグランド位置情報取得権限-->


    <application
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DDwifi4"
        tools:targetApi="31"
        >
        <meta-data android:name="osmdroid.basePath" android:value="osmdroid" />
        <meta-data android:name="osmdroid.cachePath" android:value="osmdroid/cache" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Database"
            android:exported="false" />

        <!-- MapActivityの宣言を追加 -->
        <activity
            android:name=".MapActivity"
            android:exported="false" />

        <!-- MapSettingActivityの宣言を追加-->
        <activity
            android:name=".MapSettingActivity"
            android:exported="false" />

        <activity
            android:name=".SelectRange"
            android:exported="false" />

        <activity
            android:name=".SelectPrefecture"
            android:exported="false" />


    </application>

</manifest>
